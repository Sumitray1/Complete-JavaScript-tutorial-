// 'use strict';

// const mark = {
//     first: 'Mark',
//     last: 'Ray',
//     mass: Number(prompt("enter the mass of Marks ")),
//     height: Number(prompt("Enter the height of the Mark")),
//     BMi: function () {
//         return (this.mass / (this.height * this.height));
//     }

// }
// const miller = {
//     first: 'Miller',
//     last: 'Ray',
//     mass: Number(prompt("enter the mass of Miller ")),
//     height: Number(prompt("Enter the height of the Miller")),
//     BMi: function () {
//         return (this.mass / (this.height * this.height));
//     }


// }
// console.log(`inputs data for marks and miller are\n${mark}\n${miller}`);
// if (mark.BMi() > miller.BMi()) {
//     console.log(`${mark.first} ${mark.last} BMI(${mark.BMi()}) is higher than ${miller.first} ${miller.last} miller BMI(${miller.BMi()})`);

// } else {
//     console.log(`${mark.first} ${mark.last} BMI(${mark.BMi()}) is lower than ${mark.first} ${mark.last} miller BMI(${miller.BMi()})`);

// }
// console.log(`${mark.first} ${mark.last} weight ${mark.mass} kg is ${mark.height}m tall with ${mark.BMi()} BMI`);
// console.log(`${miller.first} ${miller.last} weight ${miller.mass} kg is ${miller.height}m tall with ${miller.BMi()} BMI`);
